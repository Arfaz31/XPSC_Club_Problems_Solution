#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, p;
    cin >> n >> p;
    string s;
    for (int i = 0; i < n; i++)
    {
        cin.ignore();
        getline(cin, s);
    }
    cout << p << endl;

    return 0;
}